<Lab7:ViewBase x:Class="Lab7.DeleteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab7"
        xmlns:Lab7="clr-namespace:Lab7"
        mc:Ignorable="d"
        Title="DeleteWindow" Height="171" Width="300">
    <Grid>
        <StackPanel Margin ="10">
            <TextBlock Text="Введите имя"/>
            <TextBox Text="{Binding Name}" Margin="0 5 0 0"/>
            <TextBlock Text="Введите возраст"/>
            <TextBox Text="{Binding Age}" Margin="0 5 0 0"/>
            <StackPanel HorizontalAlignment="Right" Margin="10" Orientation="Horizontal">
                <Button  Content="Добавить" Command="{Binding AddCommand}"/>
                <Button  Content="Отмена" Margin="5 0 0 0" Command="{Binding CancelCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Lab7:ViewBase>
